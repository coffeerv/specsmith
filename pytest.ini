[pytest]
# Match by message only (no class import) so these filters survive LangChain
# refactors of its internal `_api.deprecation` module path.
filterwarnings =
    ignore:The default value of `allowed_objects` will change in a future version.*
    ignore:The class `ChatVertexAI` was deprecated in LangChain 3\.2\.0.*
