
services:
  api:
    build: .
    env_file: .env
    environment:
      LLM_PROVIDER: ${LLM_PROVIDER:-offline}
      SUPPRESS_LLM_RUNTIME_WARNINGS: ${SUPPRESS_LLM_RUNTIME_WARNINGS:-1}
    ports: ["8000:8000"]
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000
